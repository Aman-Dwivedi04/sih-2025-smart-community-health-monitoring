/**
 * A functional React component that renders a styled button.
 *
 * @returns {JSX.Element} A button element with predefined classes and attributes.
 */
const Button = () => {
    return (
        <button
            name="button"
            className="btn btn-primary btn-outline btn-square btn-lg"
        >
            click me
        </button>
    );
}

export default Button;
